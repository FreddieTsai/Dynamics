#ifndef CALCULATOR_H
#define CALCULATOR_H
#include "calculator.h"
#include<iostream>
#endif

double mass = 0, length = 0,  terminalDegree = 0, AngSpeed = 0, degree = 0, AngAcceler = 0, location = 0;

double angSpeed (double length, double degree);
double angAcceler (double length, double degree);
double calculateDegree (double length, double degree);