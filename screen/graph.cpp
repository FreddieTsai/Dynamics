/**
 * @file graph.cpp
 * @author IalvinchangI
 * @brief 在螢幕上畫出字、圖 () (`print_string`, `print_line`) (`print_char`)
 * @version 0.1
 * @date 2024-07-04
 */



#include "__screen_object.h"  // Screen
#include "__vector.h"  // position

#include "__graph.h"  // all
void print_char(position, char*);


// fill one cell by the symbol at pos
void print_char(position pos, char* symbol) {

}


// put each char in the str onto the screen at pos
void print_string(position pos, char* str) {

}


/**
 * @brief draw a line between start_pos and end_pos
 * 
 * @param start_pos where the line start
 * @param end_pos where the line end
 * @param width the width of the line
 * @param color the color of the line (L"{a char you want to print}")
 */
void print_line(position start_pos, position end_pos, int width, char* color) {

}
